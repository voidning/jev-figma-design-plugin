# 项目目标

这是 **Jev 模型 + 自然语言 + Figma 原生设计节点** 的演示插件。目标是让用户持续说话，每说完一段就尽快在画布上完成相应操作。按钮演示从一个按钮生成并排的三个按钮，再用“中间那个”“都”“太大了”等自然说法修改和纠正；圆形演示从“在这里加一个圆”继续放大并改成红色。具体句式与步骤不能写死为唯一可执行的剧本。

## Jev 接入是交付条件

- 必须真实调用 TypeSafe System One 的 `jev-latest`，让 Jev 对每个最终语音片段作受约束的意图判断。现有规则解析只能作为明确标注的离线体验或参数校验，不能冒充 Jev 接入，也不能用别的模型静默替代。
- 参考：一次调用 `POST https://api.typesafe.ai/v1/systemone`，请求包含 `model: "jev-latest"`、`state`、`questions`，用 `choice` 和置信度返回结构化判断。接口细节以 [TypeSafe AI Quick start](https://docs.typesafe.ai/introduction/quickstart) 和当前 API 响应为准。
- 密钥可在插件界面临时输入并直接提交给本机服务，也可由服务端的 `TYPESAFE_API_KEY` 读取。提交后只保存在本机服务进程内存，不写入 Figma 文档、插件存储、构建产物或 Git；服务重启后需重新输入。插件只把必要的文字指令和最小画布上下文发给本地服务。没有密钥或请求失败时明确显示 Jev 不可用；不得声称已完成真实 Jev 演示。
- Jev 用多个独立问题并行判断受支持的动作、对象、位置、目标、属性、修改方式和值。代码按动作合成通用类型化编辑命令，按“动作 × 对象 × 属性”的执行能力接口复核返回值、置信度、页面、落点、节点 ID、组件能力与变体后，才用 Figma Plugin API 修改文件。含糊或不支持的组合不执行。代码承担精确数字、单位与布局计算，不用关键词代替 Jev 的语义判断。

## 演示交互

- 持续语音听写，显示临时转写。每个完整短句或最终语音片段自动入队，按顺序、低延迟地判断并执行；不要求用户逐段点“预览”或“确认”。文字输入保留为语音不可用时的备用入口。
- 演示说法示例：“在这里放个按钮。”、“再来两个，横着排。”、“中间那个低调一点，最后那个要有危险感。”、“三个都大一点。”、“太大了。”、“好，就这样，停。” 停顿可分段触发；一句包含两个目标的改动须完整校验后再一起执行。替换成同义说法仍应得到相同编辑结果。
- 界面显示正在识别、正在判断、已执行或失败的每段步骤及实际结果。保留撤销入口，使已执行步骤可逐步撤销；重复语音事件不能重复执行同一片段。
- “这里”使用 `figma.on('drop')` 取得的当前页面画布坐标；不得凭空推测鼠标位置。没有明确拖放位置时拒绝创建。若用户从已有按钮开始，可使用明确选中的按钮。

## Figma 设计系统优先

- 搜索当前文件合适的按钮组件。找到时创建或复制 `InstanceNode`，并检查 `getMainComponentAsync()` 与 `variantProperties` / 组件属性；多个候选时让用户选择，不猜测。
- 主要、次要、危险是语义类型。优先匹配组件的对应变体或属性，并保持实例与主组件连接；有合适变量时优先使用变量。只改填充颜色不能被报告为已切换语义类型。
- 没有合适组件或变体时，明确显示缺口与可用备选。不要在未说明来源的情况下生成普通按钮或任意颜色。

## 验收与文档

- 覆盖“一个按钮 → 再加两个并排实例 → 中间次要、最后危险 → 一起放大 → 用自然语言纠正”的完整流程；另覆盖“圆形 → 放大 → 红色填充”、不同说法、等宽等高的椭圆节点、落点约束和不支持组合的拒绝。检查顺序、主组件、变体属性和逐步撤销。
- 分别验证解析、队列/去重、本地 Jev 代理、Figma 宿主行为与实际插件交互。模拟宿主或离线规则模式通过，不等于真实 Jev 请求或真实 Figma 验收通过。
- README 如实写明配置、架构、数据流、语音操作、回退模式、验证结果与尚未实测的部分；不能把计划中的能力写成已经实现。
